#include <stdio.h>
#include <math.h>

int main (){
    int N;
    scanf ("%d",&N);
        for (int i = 0; i < N; i++)
        {
            /* code */
        }
        
    int pangkat= pow (2,N);
    printf ("%d",pangkat-1);

    return 0;
} 