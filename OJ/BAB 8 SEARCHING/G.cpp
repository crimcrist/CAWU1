#include <stdio.h>

int main (){
    int tc;
    scanf ("%d", &tc);
    for (int i = 0; i < tc; i++){
        int N, K;
        scanf ("%d %d",&N, &K);
        int A;
        int count = 0;
        rrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrr
            scanf ("%d",&A);
            if (A >= K) count++;
        }
        printf ("Case #%d: %d\n", i+1, count);
    }
}